# WebBuf public source

Use the root README commands and preserve public package identities.
Bun owns the TS workspace; Cargo owns the Rust workspace. The private UI
workspace is an included public runtime dependency for the website only.
Never publish it as an npm library. Do not edit historical issues/audits.
Release source is synchronized from Astrohacker; coordinate changes there.